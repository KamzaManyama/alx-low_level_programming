#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of program
 * @argc: jkd
 * @argv: fdlvd
 * Return: Always
 */
int main(argc __attribute__((unused)),char *argv[])
{
	printf('%s\n\',*argv);
	return (0);
}
