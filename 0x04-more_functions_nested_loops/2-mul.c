#include "main.h"

/**
 * mul - checks for lowercase letters
 * @a: the chaaracyer to be checked
 * @b: the second integer
 * Return: 0
 */
int mul(int a, int b)
{
	int result = a + b;

	return (result);
}
